import React, { Component } from 'react'
import { Account, AccountInfo } from '../containers' 

export default (props) => {

  return (
    <div>
      <Account />
      <AccountInfo />
    </div>
  )
}